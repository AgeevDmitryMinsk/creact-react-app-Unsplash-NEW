import React from 'react';
import './button.css'

const Button = ({text456}) => {
    return (
        <button className="button">
            {text456}
        </button>
    )
}

export default Button;
